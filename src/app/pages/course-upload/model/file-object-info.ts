export interface IFileObjectInfo {
  fileURL: string;
  name: string;
  description: string;
  completedPercentage?: string;
}
export interface IAttachmentObjectInfo {
  fileURL: string;
  name: string;
}